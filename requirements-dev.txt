# Everything above, plus the test and lint toolchain.
-r requirements.txt

pytest>=8
ruff>=0.6
xlwt>=1.3   # tests/form_fixture.py writes the stand-in .xls; the client's real
            # form is confidential and never reaches CI
